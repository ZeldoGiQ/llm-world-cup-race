---
// Methodik in der Standardsprache (Englisch, ohne Präfix).
import MethodologyView from '../../arena/views/MethodologyView.astro';
import { DEFAULT_LOCALE } from '../../arena/lib/i18n/locales';
---

<MethodologyView locale={DEFAULT_LOCALE} />
